#include "doctors_schedule.hpp"

doctors_schedule::doctors_schedule(int num_of_appointments) {
  // Dynamically allocate the array

  // initialize _num_of_appointments

  for ( /* control the loop so it goes through all appointments */ ) {
    string name, location, time_input;
    cout << "Who is this appointment with?: ";
    getline(cin, name);
    cout << "Where is this appointment?: ";
    getline(cin, location);
    cout << "What time is this appointment?: ";
    getline(cin, time_input);
    // Create an appointment object according to user input

    // Set the appointment at this index of the array.

    cout << "\n\n";
  }
  // Initiatlize _current_appointment_num to ensure it is the first appointment.

}
